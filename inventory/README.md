# Founder personal inventory (Tier E)

Container Tree per `sops/physical_inventory.md` + `context/specs/container-inventory-v1.md`.

| Path | Purpose |
|------|---------|
| `locations/` | One `*.location.md` per physical site |
| `containers/` | One `*.container.md` per labeled box |
| `templates/` | Copy `container.container.md` for new boxes |

**Active:** `LOC-TH-PHUKET-HOME` — founder-backlog **T-020**.

**Documents:** use dedicated containers (`CN-TH-DOC-*`) for passports, contracts, title copies — not mixed with misc boxes without explicit rows.
