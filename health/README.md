# Founder health — Track A folder

| Path | Purpose |
|------|---------|
| `context-log.md` | **Canonical clinical + recovery context** (episodes, protocols, triggers). Agents: read §4 for planner inputs only; never surface §1–3 clinically outside Tier E |
| `template.md` | Daily vitality log template |
| `YYYY-MM-DD.md` | Daily logs |
| `support-network.md` | Support contacts + **pre-gen codes** for Lana post-session |
| `sessions/` | Per-session notes (Tier E) |

**Pleasant capture rule:** after support sessions → reply Lana with codes (`A, 1, X`) or edit session file — no essay required.

**Current (2026-07-21):** recovery / asthenia phase — see `context-log.md` §4 for OS planner constraints.
